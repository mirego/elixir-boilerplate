# NOTE: This file should always be empty.
#
# Production-specific configuration should go in the special `MIX_ENV == "prod"`
# block in `rel/config/config.exs` since we use an OTP release to deploy and
# run the application in production.
#
# Configuration specified in this file would only be picked up at runtime when
# running `mix phx.server` in production, which we never do.
